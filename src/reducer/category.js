const initialState = {
    0: {
        id: 0,
        name: "Kopi"
    },
    1: {
        id: 1,
        name: "Makanan Instan"
    }
}

export default (state = initialState, action) => {
    return state;
}
